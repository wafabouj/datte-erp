# En local avec le proxy Vite (npm run dev), laissez cette variable absente ou vide.
# En production (build déployé séparément du backend), pointez vers l'URL publique
# de l'API, terminée par /api :
# VITE_API_URL=https://votre-backend.onrender.com/api
